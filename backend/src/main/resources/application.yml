spring:
  application:
    name: iam-backend
  security:
    oauth2:
      resourceserver:
        jwt:
          # W2 #14 — Backend as OAuth2 resource server pointing at the cybergroup realm.
          # JWK set URI is auto-derived from the issuer-uri.
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/cybergroup}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"

# Admin dashboard -> Keycloak Admin REST API. The backend relays the calling admin's
# own bearer token (no client secret here), so this only needs Keycloak's internal
# base URL. Lab: http://10.0.20.10:8080 ; local docker: http://keycloak:8080.
app:
  keycloak:
    base-url: ${KEYCLOAK_BASE_URL:http://localhost:8080}
    realm: cybergroup
