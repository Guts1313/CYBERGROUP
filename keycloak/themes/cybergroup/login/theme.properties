# CYBERGROUP login theme — dark, matches the IAM Demo Console palette.
# Extends the stock keycloak login theme and layers cybergroup.css on top
# (patternfly base is inherited via the parent's stylesCommon).
parent=keycloak
import=common/keycloak
styles=css/login.css css/cybergroup.css
